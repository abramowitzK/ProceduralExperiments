#include "world.hpp"
#include <resource_manager.hpp>
namespace Aurora {
	void World::init(Vector3 cameraPos) {
		auto instance = ResourceManager::instance();
		for (int i = 0; i < 4; i++) {
		
		}
	}
	void World::update(double dt, Vector3 cameraPos) {
		
	}
	void World::render(Renderer* renderer) {
		
	}
}
